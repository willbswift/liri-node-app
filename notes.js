
	// This creates an array with only the new data that you fed into node.
let args = process.argv.slice(2);

	// allows us to write data to other files
let fs = require("fs");

	// turns data into an array with commas marking out each item
let things = data.toString().split(",");

	//goes through an array and does the same thing to each item in the array
.forEach( function( thing ) goes here )


